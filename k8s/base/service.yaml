apiVersion: v1
kind: Service
metadata:
  name: modular-cicd-app-service
spec:
  selector:
    app: modular-cicd-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort
